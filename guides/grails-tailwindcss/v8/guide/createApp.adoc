Generate a fresh Apache Grails 8 web application from link:https://grails.apache.org/start/[grails.apache.org/start]. This is the same forge that produced the `initial/` tree in the upstream repository.

Use the `web` profile. That keeps you on the same starter layout, index page, asset-pipeline manifest, and dependency set that Grails 8 ships from `grails-profiles/web`.

Files touched in this chapter:

* `build.gradle`
* `grails-app/views/layouts/main.gsp`
* `grails-app/views/index.gsp`
