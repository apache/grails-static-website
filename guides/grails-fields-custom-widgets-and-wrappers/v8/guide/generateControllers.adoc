Generate two controllers, one per domain class:

[source,bash]
----
./grailsw create-controller example.library.Book
./grailsw create-controller example.library.Author
----

Replace each generated controller with the scaffolded version below. The `static scaffold` property is the only line you need:

[source,groovy]
.grails-app/controllers/example/library/BookController.groovy
----
include::../snippets/grails-app/controllers/example/library/BookController.groovy[]
----

[source,groovy]
.grails-app/controllers/example/library/AuthorController.groovy
----
include::../snippets/grails-app/controllers/example/library/AuthorController.groovy[]
----

Restart the app, browse to `http://localhost:8080/book/index`, and you should see a working CRUD UI rendered entirely from runtime-generated GSPs.
