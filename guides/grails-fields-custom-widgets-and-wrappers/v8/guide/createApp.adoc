Generate a fresh Grails 8 snapshot project. The link:https://prev-snapshot.grails.org[Grails snapshot starter service] exposes the same `create-app` API as `start.grails.org` but for the in-development branch.
