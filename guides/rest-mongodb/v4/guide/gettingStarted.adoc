In this guide you are going to build a https://grails.apache.org[Grails application] that uses http://gorm.grails.org/latest/mongodb[GORM for MongoDB] to access http://mongodb.org[MongoDB] and produce a JSON response in a RESTful manner.
